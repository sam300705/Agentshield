# AgentShield Feature Matrix

**Audit basis:** follow-up branch `agent/final-product-hardening`, based on verified PR #2 head `94d6e17`. This matrix separates verified local/CI behavior from configuration-required or unimplemented production lifecycle work. It is an engineering audit artifact, not a production-readiness claim.

| Feature                                                 | UI                                                                                                                                                                           | API                                                                                          | Database                                                                                                    | Worker                                                                    | Tests                                                                                  | Deployment                                                                    | Security                                                                                  | Current status                                                     |
| ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Deterministic demo dashboard                            | Functional overview, seeded panels, policy simulation, attack-graph/flight-recorder/approval/receipt demo interactions                                                       | Demo data is client-local; live summary is separate                                          | No live data dependency in demo mode                                                                        | Not required for client demo                                              | Dashboard unit tests and production build pass                                         | Static Vercel preview verified                                                | Explicitly labeled deterministic demo; no live customer-data claim                        | **Implemented and verified for demo**                              |
| Live dashboard summary/history                          | `LiveDashboard` loads authenticated summary and scan history with loading, retry, empty, 401, and 403 states                                                                 | `GET /api/dashboard/summary`, `GET /api/scans`                                               | Tenant-scoped scan queries                                                                                  | Reads completed scan records                                              | API client and dashboard tests pass                                                    | Requires configured API origin and OIDC                                       | Bearer token is explicit and memory-only; cookies omitted                                 | **Implemented; requires live API/OIDC configuration**              |
| Manual repository registration and real repository scan | No production repository onboarding screen                                                                                                                                   | No real repository registration/manual scan endpoint; only demo enqueue                      | Repository model exists but is not connected to a real ingestion lifecycle                                  | Queue execution is hard-wired to the demo target                          | Demo queue/integration coverage only                                                   | No live repository source configuration                                       | Safe local scanner does not execute repository code                                       | **Not implemented; owner/configuration boundary remains**          |
| PostgreSQL scan queue                                   | No queue administration UI                                                                                                                                                   | Durable enqueue, claim, retry, stale-lock recovery, cancellation primitives                  | Queue/job fields are persisted and tenant/correlation scoped                                                | Worker supports long-running and once-drain modes, but executes demo scan | Integration and queue behavior tests pass                                              | Deployment runbook exists; no live worker/database deployed                   | Bounded attempts, stale-lock recovery, tenant context, correlation IDs                    | **Implemented and locally verified; not remotely deployed**        |
| Production authentication                               | Live auth gate and session-expiry/forbidden states                                                                                                                           | Backend OIDC JWT verification with issuer, audience, JWKS, role/org checks                   | User/org/membership models exist                                                                            | Worker shares validated configuration                                     | Negative backend auth tests and frontend OIDC tests pass                               | Requires provider registration and exact environment values                   | Fail-closed production mode; demo auth explicitly non-production                          | **Implemented; requires owner configuration**                      |
| Browser OIDC session                                    | Authorization-code PKCE, state/nonce, reload-safe short-lived transaction storage, logout                                                                                    | Token endpoint adapter                                                                       | No browser token persistence in local/session storage; transaction metadata only in session storage         | Not applicable                                                            | Signed JWT/JWKS, reload, expiry, callback, refresh tests pass                          | Requires `VITE_OIDC_*`, including `VITE_OIDC_JWKS_URI`                        | jose signature/JWKS verification; no nonstandard `id_token_claims` trust path             | **Implemented; requires owner configuration**                      |
| Tenant/RBAC control plane                               | Live screens expose protected data only when authenticated                                                                                                                   | Organization-scoped controllers and permission checks                                        | Tenant-scoped scans, findings, SBOM, approvals, audits, aggregates                                          | Queue jobs carry organization/correlation context                         | RBAC, tenant, and integration smoke coverage pass                                      | Requires production identity/org claim mapping                                | Fail-closed unknown organization; server-side permissions                                 | **Implemented and verified locally**                               |
| Approval cockpit                                        | Demo approval interaction; live API supports pending list/mutation                                                                                                           | List/approve/reject endpoints                                                                | Approval state and audit records                                                                            | Not worker-dependent                                                      | Separation-of-duties and atomic transition tests pass                                  | Live use requires API/OIDC                                                    | Requester self-approval blocked server-side; tenant scoped; correlated audit              | **Implemented; dashboard live mutation workflow remains limited**  |
| Scanner and evidence handling                           | Findings are shown in demo/live views where API data exists                                                                                                                  | Scan lifecycle persists findings                                                             | Finding evidence persisted with shared sanitizer                                                            | Safe bounded traversal; no repository code execution                      | Secret, agent-workflow, integration, SARIF, and redaction tests pass                   | CLI/CI verified; cloud worker not live                                        | Raw secrets are not persisted in covered boundaries; fingerprints remain                  | **Implemented and verified for current scanner scope**             |
| OSV vulnerability intelligence                          | No live advisory dashboard lifecycle                                                                                                                                         | Adapter/CLI boundary exists; API scan lifecycle does not invoke it                           | No dedicated advisory persistence lifecycle                                                                 | Not enabled by default in worker                                          | Mocked adapter and CLI tests pass                                                      | Optional network CLI only; no production provider wiring                      | Timeout/retry/cache/bounded behavior in adapter; outage policy not product-wired          | **Adapter implemented; lifecycle not connected**                   |
| GitHub App onboarding/webhooks                          | No live installation UI                                                                                                                                                      | Signature, replay, event parsing, ownership primitives; no public webhook lifecycle endpoint | Installation/delivery persistence is not complete                                                           | Webhook-to-scan enqueue is not connected                                  | Safe HMAC/replay/ownership tests pass                                                  | No App registered or installed                                                | Raw-body HMAC boundary and replay primitive exist; current replay is not durable          | **Security boundary implemented; product lifecycle not connected** |
| Signed security receipts                                | Demo export interaction exists; live export is not connected                                                                                                                 | Receipt primitives and offline CLIs exist                                                    | SecurityReceipt model exists; actual scan lifecycle persistence/signing is not connected                    | Not worker-connected                                                      | Canonicalization, tampering, wrong-key, and rotation tests pass                        | Key custody/export not deployed                                               | Private keys read locally only; no key committed or printed                               | **Primitives verified; product lifecycle not connected**           |
| Distributed rate limiting                               | No administration UI                                                                                                                                                         | Redis-compatible adapter exists; API still uses per-instance default                         | No shared store dependency                                                                                  | Not worker-dependent                                                      | Outage and header behavior tests pass                                                  | No vendor/store configured                                                    | Explicit fail-open/fail-closed policy and bounded local fallback                          | **Adapter verified; runtime wiring not connected**                 |
| Observability                                           | Limited dashboard status/error states                                                                                                                                        | Health/readiness/metrics and correlation IDs exist                                           | Operational events/audits persisted where applicable                                                        | Queue/scan metadata available                                             | Smoke/integration checks cover health and correlation behavior                         | Deployment documentation exists; no remote telemetry backend                  | Sensitive values excluded from covered evidence/error paths                               | **Foundation implemented; production telemetry wiring remains**    |
| Database hardening                                      | Not user-visible                                                                                                                                                             | Prisma service boundaries                                                                    | Relations, tenant fields, indexes, migrations exist; richer advisory/webhook lifecycle models remain absent | Queue uses atomic database operations                                     | Migration deploy and integration smoke pass                                            | Local PostgreSQL verified; Neon not connected                                 | No `db push`-only production claim; direct migration URL documented                       | **Current schema verified for implemented scope**                  |
| Browser E2E                                             | Playwright covers current demo boot, navigation, command palette, simulation, replay, accessible graph fallback, approval, receipt export, keyboard use, and mobile viewport | No browser-driven live API harness                                                           | Demo browser path is deterministic/client-local                                                             | Not applicable                                                            | `pnpm test:e2e` passes 6 real-browser tests locally; CI installation/run is configured | Static Vercel preview can be exercised; CI browser installation is configured | Failure screenshots/traces are retained; tests do not claim absent live workflows         | **Implemented and locally verified; remote CI pending**            |
| Supply-chain/container verification                     | Not user-visible                                                                                                                                                             | CI workflow runs repository checks                                                           | Frozen lockfile and migrations                                                                              | Container path documented                                                 | CI quality job passes; Docker unavailable in sandbox                                   | Container build remains unverified locally                                    | Minimal workflow permissions and source SARIF gate; action deprecation annotations remain | **Partially verified; container/action maintenance remains**       |

## Merge-readiness snapshot

PR #2 is open, non-draft, mergeable, and clean. The branch is 42 commits ahead of `main` and has no recorded branch protection on `main`. The latest GitHub Actions quality run and Vercel check passed for the preceding verified PR #2 head; this follow-up branch adds E2E and CI changes that require a new remote run. The branch has not been merged, and no merge should occur without owner approval.

The strongest technically honest classification is **safe to merge after the final documentation/verification commit passes remote checks, with owner approval required**. That classification means the code change set is reviewable and CI-verifiable; it does not mean the external API, worker, database, OIDC provider, GitHub App, OSV lifecycle, signing custody, or shared limiter are live.

## Highest-priority safe follow-up work

1. Add a real browser smoke suite for the current deterministic demo and explicit failure states without fabricating absent product workflows.
2. Keep synthetic fixtures runtime-assembled and retain only the narrow examples/controller/template exclusions that the current scanner identifies; the credential test files now pass the source self-scan without exclusion.
3. Add shared response schemas and contract tests for the existing API endpoints before introducing new live repository lifecycle endpoints.
4. Prepare, but do not activate, durable webhook delivery/install persistence and repository-scan lifecycle schemas only if the end-to-end ownership and retry semantics are fully specified.
5. Keep all owner-required integrations clearly configuration-gated and fail-closed.
