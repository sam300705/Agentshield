{
  "buildCommand": "pnpm --filter @agentshield/web-dashboard build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/web-dashboard/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
